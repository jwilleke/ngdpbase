---
title: Demo Welcome
uuid: 9fe8c86d-2a45-4630-bce2-94800fe26cd4
system-category: documentation
user-keywords:
  - Demo
  - Getting Started
slug: demo-welcome
lastModified: '2026-08-06T00:00:00.000Z'
author: system
author-lock: true
---
# Demo Welcome

This is a **public demo** of [ngdpbase|https://github.com/jwilleke/ngdpbase|target='_blank'].

%%error
Everything here is real software — the same build anyone can run — pointed at a throwaway data volume.
%%

Read anything without signing in. To try **editing**, you need an account.

## Getting an account

Sign in with your **email address**. No password is involved, and none is ever created for you:

1. Go to the sign-in page and enter your email.
2. A one-time link arrives in your inbox.
3. Click it. That's it — you're in, and an account is created on first use.

The link expires after a few minutes and works once. If it expires, request another.

Accounts made this way have **no password at all**, so there is nothing to forget and nothing to leak.

## What you can do once signed in

- **Edit** any existing page
- **Create** new pages
- **Upload** attachments and media

What you cannot do is delete or rename pages. That is deliberate — a shared demo where anyone can delete anything does not stay useful for long.

## Please read before you write

%%error
This is a public demo. Treat everything here as visible to strangers.
/%

Your email address is visible. Anyone signed in as the demo administrator can see the account list. **Use a throwaway address** if that matters to you.

- Every page is world-readable**, including anything you write.
- **Nothing is backed up.** The site can be reset to a clean slate at any time and your work will go with it, without warning.

Do not put anything private, personal or confidential on this instance.

The [Demo Sandbox] page exists specifically for experimenting. Start there.

## Trying the admin dashboard

Sign in as **`admindemo`**, password **`admin123`**, to look around `/admin` — the trash view, required-pages sync, configuration, the roles and permissions model.

It is **read-only**. Every screen is fully populated and nothing you click will change anything: the account holds `admin-read` and not `admin-system`, so all administrative actions are refused. Configuration secrets are masked with no way to reveal them.

It is also **shared with every other visitor**, so its password, email address and display name are frozen — you will be refused if you try to change them from your profile. Everything else about the account is yours to play with.

## Where to go next

- [Demo Sandbox] — scribble here, it's meant to be broken
- [Demo Feature Tour] — what the markup can actually do
- [Using Links] — how to link pages and sections
- [Markdown Cheat Sheet] — general syntax reference

## Is this the real thing?

Yes. This is not a mock-up or a limited trial — it is a normal instance with the demo settings turned on. The differences from a private install are only these:

- registration is by email link only
- edits are limited to creating and changing pages
- and the data is disposable.
