{
  "vscodeGrok.showPreview": "always",
  "git.confirmSync": false,
  "chat.agentSessionsViewLocation": "view",
  "chat.edits2.enabled": true,
  // Rules come from the canonical .markdownlint.jsonc (single source of truth);
  // no inline markdownlint.config so editor, CLI, CI and hooks stay identical.
  "markdownlint.ignore": ".markdownlintignore",
  "files.associations": {
    ".markdownlintignore": "ignore"
  }
}