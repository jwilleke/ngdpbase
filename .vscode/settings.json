{
  "vscodeGrok.showPreview": "always",
  "git.confirmSync": false,
  "chat.agentSessionsViewLocation": "view",
  "chat.edits2.enabled": true,
  // Rules, globs and ignores are .markdownlint-cli2.jsonc — same file as
  // npm run lint:md and the GitHub markdown-lint / CI lint jobs. Do not add
  // a second markdownlint.config or a .markdownlint.json here.
}