<%# #794 — generic page editor. Form + frontmatter + body editor +
    attachments + scripts live in `_basicEditor.ejs` so addon-specific
    editor templates can include the same partial with
    `extraFrontmatterFields` / `extraBodyControls` / `extraSubmitActions`
    slot variables. This shim renders with empty slots, preserving
    prior behaviour for all non-addon pages. %>
<%- include('_basicEditor') %>
