
            <!-- Dynamic Footer from Footer.md -->
            <% if (typeof footer !== 'undefined' && footer) { %>
                <div class="footer-content mt-4 pt-3 border-top">
                    <%- footer %>
                </div>
            <% } %>

            <!-- #670 Phase A: Contact link in the footer when contact is fully available -->
            <% if (typeof contactAvailable !== 'undefined' && contactAvailable && (typeof contactFooterEnabled === 'undefined' || contactFooterEnabled)) { %>
                <div class="footer-contact mt-3 pt-2 border-top text-center small text-muted">
                    <a href="/contact"><i class="fas fa-envelope"></i> Contact</a>
                </div>
            <% } %>

            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
    <script>hljs.highlightAll();</script>
    <!-- theme-manager.js removed: SimpleThemeManager in header.ejs handles theming (#377) -->
    <!-- Page Link Autocomplete - Issue #90 (loaded globally) -->
    <script src="/js/page-autocomplete.js"></script>
    <!-- Emoji Autocomplete - Issue #512 (loaded globally, used in edit.ejs) -->
    <script src="/js/emoji-autocomplete.js"></script>
    <!-- My Links pinned pages - Issue #537 -->
    <script src="/js/my-links.js"></script>

    <script>
    // Initialize user preferences if available
    if (typeof currentUser !== 'undefined' && currentUser && currentUser.preferences) {
        window.userPrefs = new UserPreferences(currentUser.preferences);
        
        // Apply display preferences
        document.addEventListener('DOMContentLoaded', function() {
            window.userPrefs.applyDisplayPreferences();
            
            // Apply editor preferences to any textareas
            document.querySelectorAll('textarea').forEach(function(textarea) {
                window.userPrefs.applyEditorPreferences(textarea);
            });
        });
    }
    </script>
</body>
</html>
