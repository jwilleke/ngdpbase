<%# #795 — generic page view. Layout + body content live in
    `_basicView.ejs` so addon-specific view templates can include the
    same partial with `extraBodyPreContent` / `extraBodyPostContent`
    slot variables. This shim renders with empty slots, preserving
    the prior behaviour for all non-addon pages. %>
<%- include('_basicView') %>
