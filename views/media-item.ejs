<%- include('header', { pageName: (item.metadata && item.metadata.caption) ? item.metadata.caption : item.filename }) %>
<%
  const _navParts = [];
  if (albumKeyword) _navParts.push('keyword=' + encodeURIComponent(albumKeyword));
  if (sortParam) _navParts.push(sortParam);
  const navSuffix = _navParts.length ? '?' + _navParts.join('&') : '';
%>

<%
  // #149: choose schema.org type based on MIME — ImageObject / VideoObject / MediaObject fallback.
  const _mt = item.mimeType || '';
  const _itemType = _mt.startsWith('image/')
    ? 'https://schema.org/ImageObject'
    : (_mt.startsWith('video/') ? 'https://schema.org/VideoObject' : 'https://schema.org/MediaObject');
%>
<div class="container mt-4" itemscope itemtype="<%= _itemType %>">
    <meta itemprop="identifier" content="<%= item.id %>">
    <meta itemprop="url" content="/media/item/<%= item.id %>">
    <% if (_mt) { %><meta itemprop="encodingFormat" content="<%= _mt %>"><% } %>
    <% if (item.metadata && item.metadata.dateTimeOriginal) { %><meta itemprop="dateCreated" content="<%= item.metadata.dateTimeOriginal %>"><% } %>
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 itemprop="name"><i class="fas <%= item.mimeType && item.mimeType.startsWith('video/') ? 'fa-film' : 'fa-image' %>"></i> <%= (item.metadata && item.metadata.caption) ? item.metadata.caption : item.filename %></h1>
                <div>
                    <% if (item.year) { %>
                        <a href="/media/year/<%= item.year %>" class="btn btn-sm me-2 btn-secondary">
                            <i class="fas fa-calendar-alt"></i> <%= item.year %>
                        </a>
                    <% } %>
                    <% if (albumKeyword) { %>
                        <% const backSuffix = sortParam ? '?' + sortParam : ''; %>
                        <a href="/media/keyword/<%= encodeURIComponent(albumKeyword) %><%= backSuffix %>" class="btn btn-sm btn-secondary">
                            <i class="fas fa-arrow-left"></i> <%= albumKeyword %>
                        </a>
                    <% } else { %>
                        <a href="/media" class="btn btn-sm btn-secondary">
                            <i class="fas fa-arrow-left"></i> Media
                        </a>
                    <% } %>
                </div>
            </div>

            <div class="row">
                <!-- Image / video preview -->
                <div class="col-md-8 mb-4">
                    <meta itemprop="contentUrl" content="/media/file/<%= item.id %>">
                    <% if (item.mimeType && item.mimeType.startsWith('image/')) { %>
                        <img
                            src="/media/thumb/<%= item.id %>?size=800x600"
                            class="img-fluid rounded shadow"
                            alt="<%= item.filename %>"
                            style="max-width:100%;"
                            itemprop="thumbnail"
                        >
                    <% } else if (item.mimeType && item.mimeType.startsWith('video/')) { %>
                        <video
                            controls
                            preload="metadata"
                            style="max-width:100%; width:100%; border-radius:0.375rem; box-shadow:0 .5rem 1rem rgba(0,0,0,.15);"
                        >
                            <source src="/media/file/<%= item.id %>" type="<%= item.mimeType %>" itemprop="contentUrl">
                            Your browser does not support the video tag.
                        </video>
                    <% } else { %>
                        <div class="d-flex align-items-center justify-content-center bg-body-secondary rounded" style="min-height:300px;">
                            <div class="text-center text-muted">
                                <i class="fas fa-file fa-5x mb-3"></i>
                                <p><%= item.filename %></p>
                                <a href="/media/file/<%= item.id %>" class="btn btn-sm btn-secondary">Download</a>
                            </div>
                        </div>
                    <% } %>
                    <% if (prevItem || nextItem) { %>
                    <div class="d-flex justify-content-between mt-2">
                        <% if (prevItem) { %>
                            <a href="/media/item/<%= prevItem.id %><%= navSuffix %>" class="btn btn-sm btn-secondary" title="<%= prevItem.filename %>">
                                &laquo; Prev
                            </a>
                        <% } else { %>
                            <span></span>
                        <% } %>
                        <% if (nextItem) { %>
                            <a href="/media/item/<%= nextItem.id %><%= navSuffix %>" class="btn btn-sm btn-secondary" title="<%= nextItem.filename %>">
                                Next &raquo;
                            </a>
                        <% } %>
                    </div>
                    <% } %>
                </div>

                <!-- Metadata panel -->
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header"><i class="fas fa-info-circle"></i> Details</div>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item d-flex justify-content-between">
                                <span class="text-muted">Filename</span>
                                <span class="text-end" style="max-width:60%;overflow-wrap:break-word;"><%= item.filename %></span>
                            </li>
                            <% if (item.year) { %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span class="text-muted">Year</span>
                                <a href="/media/year/<%= item.year %>" class="text-info"><%= item.year %></a>
                            </li>
                            <% } %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span class="text-muted">Type</span>
                                <span><%= item.mimeType %></span>
                            </li>
                            <% if (item.metadata && item.metadata.dateTimeOriginal) { %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span class="text-muted">Date</span>
                                <span><%= item.metadata.dateTimeOriginal %></span>
                            </li>
                            <% } %>
                            <% if (item.metadata && item.metadata.make) { %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span class="text-muted">Camera</span>
                                <span><%= item.metadata.make %><% if (item.metadata.model) { %> <%= item.metadata.model %><% } %></span>
                            </li>
                            <% } %>
                            <% if (item.metadata && item.metadata.title) { %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span class="text-muted">Title</span>
                                <span><%= item.metadata.title %></span>
                            </li>
                            <% } %>
                            <% if (item.metadata && item.metadata.description) { %>
                            <li class="list-group-item">
                                <span class="text-muted d-block">Description</span>
                                <span itemprop="description"><%= item.metadata.description %></span>
                            </li>
                            <% } %>
                            <% if (item.metadata && item.metadata.keywords) { %>
                            <li class="list-group-item">
                                <span class="text-muted d-block">Keywords</span>
                                <span>
                                    <% const kw = Array.isArray(item.metadata.keywords) ? item.metadata.keywords : [item.metadata.keywords]; %>
                                    <% kw.forEach(function(k, i) { var exists = keywordPageExists && keywordPageExists[k]; var href = exists ? '/view/' + encodeURIComponent(k) : '/edit/' + encodeURIComponent(k); %><a href="<%= href %>" class="<%= exists ? 'text-info' : 'redlink' %>" itemprop="keywords"<% if (!exists) { %> title="Create page: <%= k %>"<% } %>><%= k %></a> <a href="/media/keyword/<%= encodeURIComponent(k) %>" class="text-muted ms-1" title="View media album for: <%= k %>"><i class="fas fa-images" style="font-size:0.85em;"></i></a><% if (i < kw.length - 1) { %>, <% } %><% }); %>
                                </span>
                            </li>
                            <% } %>
                            <% if (item.metadata && (item.metadata.gpsLatitude || item.metadata.gpsLongitude)) { %>
                            <% const lat = parseFloat(item.metadata.gpsLatitude); const lon = parseFloat(item.metadata.gpsLongitude); %>
                            <li class="list-group-item d-flex justify-content-between" itemprop="contentLocation" itemscope itemtype="https://schema.org/Place">
                                <span class="text-muted">GPS</span>
                                <span class="location-plugin" itemprop="geo" itemscope itemtype="https://schema.org/GeoCoordinates"><meta itemprop="latitude" content="<%= lat %>"><meta itemprop="longitude" content="<%= lon %>"><a href="https://www.openstreetmap.org/?mlat=<%= lat %>&mlon=<%= lon %>#map=13/<%= lat %>/<%= lon %>" target="_blank" rel="noopener" class="location-link text-info" title="Open in OpenStreetMap"><i class="fas fa-map-marker-alt"></i> <%= lat.toFixed(4) %>, <%= lon.toFixed(4) %></a></span>
                            </li>
                            <% } %>
                            <% if (item.linkedPageName) { %>
                            <li class="list-group-item d-flex justify-content-between">
                                <span class="text-muted">Page</span>
                                <a href="/view/<%= item.linkedPageName %>" class="text-info"><%= item.linkedPageName %></a>
                            </li>
                            <% } %>
                        </ul>
                    </div>

                    <% if (item.dirPath) { %>
                    <div class="mt-3">
                        <small class="text-muted"><i class="fas fa-folder"></i> <%= item.dirPath %></small>
                    </div>
                    <% } %>

                    <!-- Media Information (collapsible) -->
                    <div class="mt-3">
                        <button class="btn btn-sm w-100 text-start btn-secondary"
                                type="button"
                                data-bs-toggle="collapse"
                                data-bs-target="#mediaInfoPanel"
                                aria-expanded="false">
                            <i class="fas fa-info-circle"></i> Media Information
                        </button>
                        <div class="collapse" id="mediaInfoPanel">
                            <div class="card mt-1">
                                <ul class="list-group list-group-flush">
                                    <li class="list-group-item">
                                        <span class="text-muted d-block" style="font-size:0.8em;">Item ID</span>
                                        <span class="font-monospace" style="font-size:0.85em; word-break:break-all;"><%= item.id %></span>
                                        <button class="btn btn-sm btn-outline-secondary ms-1 py-0 px-1" title="Copy ID" onclick="copyText('<%= item.id %>')"><i class="fas fa-copy"></i></button>
                                    </li>
                                    <li class="list-group-item">
                                        <span class="text-muted d-block" style="font-size:0.8em;">URL</span>
                                        <span class="font-monospace" style="font-size:0.85em;">/media/item/<%= item.id %></span>
                                        <button class="btn btn-sm btn-outline-secondary ms-1 py-0 px-1" title="Copy URL" onclick="copyText(window.location.origin + '/media/item/<%= item.id %>')"><i class="fas fa-copy"></i></button>
                                    </li>
                                    <li class="list-group-item">
                                        <span class="text-muted d-block" style="font-size:0.8em;">Plugin Syntax</span>
                                        <% const pluginKws = item.metadata && item.metadata.keywords ? (Array.isArray(item.metadata.keywords) ? item.metadata.keywords : [item.metadata.keywords]) : []; %>
                                        <% if (pluginKws.length > 0) { %>
                                            <% pluginKws.forEach(function(kw) { const q = String(kw).includes("'") ? '"' : "'"; const syn = "[{MediaPlugin format='list' keyword=" + q + kw + q + "}]"; %>
                                            <div class="d-flex align-items-center mb-1">
                                                <span class="font-monospace" style="font-size:0.85em;flex:1;word-break:break-all;"><%= syn %></span>
                                                <button class="btn btn-sm btn-outline-secondary ms-1 py-0 px-1 flex-shrink-0" title="Copy syntax" data-copy="<%= syn %>" onclick="copyText(this.dataset.copy)"><i class="fas fa-copy"></i></button>
                                            </div>
                                            <% }); %>
                                        <% } else { %>
                                            <div class="d-flex align-items-center">
                                                <span class="font-monospace" style="font-size:0.85em;">[{MediaPlugin format='list' keyword='current'}]</span>
                                                <button class="btn btn-sm btn-outline-secondary ms-1 py-0 px-1" title="Copy syntax" onclick="copyText(&quot;[{MediaPlugin format='list' keyword='current'}]&quot;)"><i class="fas fa-copy"></i></button>
                                            </div>
                                        <% } %>
                                    </li>
                                    <% if (item.filePath) { %>
                                    <li class="list-group-item">
                                        <span class="text-muted d-block" style="font-size:0.8em;">File Path</span>
                                        <span class="font-monospace" style="font-size:0.75em; word-break:break-all;"><%= item.filePath %></span>
                                        <button class="btn btn-sm btn-outline-secondary ms-1 py-0 px-1" title="Copy path" data-copy="<%= item.filePath %>" onclick="copyText(this.dataset.copy)"><i class="fas fa-copy"></i></button>
                                    </li>
                                    <% } %>
                                    <% if (item.metadata && item.metadata.imageWidth && item.metadata.imageHeight) { %>
                                    <li class="list-group-item d-flex justify-content-between">
                                        <span class="text-muted">Dimensions</span>
                                        <span><%= item.metadata.imageWidth %> × <%= item.metadata.imageHeight %></span>
                                    </li>
                                    <% } %>
                                    <% if (item.metadata && item.metadata.fileSize) { %>
                                    <li class="list-group-item d-flex justify-content-between">
                                        <span class="text-muted">File Size</span>
                                        <span><%= Math.round(Number(item.metadata.fileSize) / 1024) %> KB</span>
                                    </li>
                                    <% } %>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
// Keyboard navigation
(function () {
    const prev = '<% if (prevItem) { %>/media/item/<%= prevItem.id %><%= navSuffix %><% } %>';
    const next = '<% if (nextItem) { %>/media/item/<%= nextItem.id %><%= navSuffix %><% } %>';
    document.addEventListener('keydown', function (e) {
        if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') return;
        if (e.key === 'ArrowLeft'  && prev) { window.location.href = prev; }
        if (e.key === 'ArrowRight' && next) { window.location.href = next; }
    });

    // Touch swipe navigation (left = next, right = prev)
    var _swipeX = 0, _swipeY = 0;
    document.addEventListener('touchstart', function (e) {
        _swipeX = e.changedTouches[0].screenX;
        _swipeY = e.changedTouches[0].screenY;
    }, { passive: true });
    document.addEventListener('touchend', function (e) {
        var dx = e.changedTouches[0].screenX - _swipeX;
        var dy = e.changedTouches[0].screenY - _swipeY;
        if (Math.abs(dx) > 50 && Math.abs(dx) > Math.abs(dy) * 1.5) {
            if (dx < 0 && next) { window.location.href = next; }
            if (dx > 0 && prev) { window.location.href = prev; }
        }
    }, { passive: true });
})();

window.ngdpbaseMediaItem = <%- JSON.stringify({
    id: item.id,
    filename: item.filename,
    filePath: item.filePath || null,
    mimeType: item.mimeType || null,
    keywords: (item.metadata && item.metadata.keywords) ? (Array.isArray(item.metadata.keywords) ? item.metadata.keywords : [item.metadata.keywords]) : [],
    imageWidth: (item.metadata && item.metadata.imageWidth) ? item.metadata.imageWidth : null,
    imageHeight: (item.metadata && item.metadata.imageHeight) ? item.metadata.imageHeight : null,
    fileSize: (item.metadata && item.metadata.fileSize) ? item.metadata.fileSize : null
}) %>;
</script>

<%- include('footer') %>
