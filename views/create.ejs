<%- include('header', { pageName: '' }) %>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <h1>Create New Page</h1>
            
            <form action="/create" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <!-- Page Name and Template on same row -->
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label for="pageName" style="font-weight:bold;">Page Name:</label>
                        <input type="text" id="pageName" name="pageName" class="form-control" 
                               value="<%= typeof pageName !== 'undefined' ? pageName : '' %>" 
                               placeholder="Enter page name..." required>
                        <small class="form-text text-muted">Use spaces for readability. Special characters will be handled automatically.</small>
                    </div>
                    <div class="col-md-6">
                        <label for="templateName" style="font-weight:bold;">Template:</label>
                        <select id="templateName" name="templateName" class="form-control" required>
                            <option value="">Select a template...</option>
                            <% templates.forEach(template => { %>
                                <option value="<%= template.name %>" <%= template.name === 'default' ? 'selected' : '' %>><%= template.displayName || template.name %></option>
                            <% }); %>
                        </select>
                    </div>
                </div>
                
                <!-- Categories and User Keywords on same row -->
                <div class="row mb-3">
                    <% if (user && user.roles && user.roles.includes('admin')) { %>
                    <div class="col-md-6">
                        <label style="font-weight:bold;">System Category (Admin Only):</label>
                        <select class="form-control" id="systemCategory" name="system-category" required>
                            <% systemCategories.forEach(category => { %>
                                <option value="<%= category %>" <%= category === defaultCategory ? 'selected' : '' %>><%= category %></option>
                            <% }); %>
                        </select>
                    </div>
                    <% } else { %>
                    <div class="col-md-6">
                        <label style="font-weight:bold;">System Category:</label>
                        <input type="text" class="form-control" id="systemCategory" name="system-category" value="<%= defaultCategory %>" readonly>
                    </div>
                    <% } %>
                    <div class="col-md-6">
                        <label style="font-weight:bold;">User Keywords (Optional, Max <%= maxUserKeywords %>):</label>
                        <div class="dropdown">
                            <button class="btn btn-outline-secondary dropdown-toggle w-100 text-start" type="button" id="userKeywordsDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                                <span id="selectedUserKeywordsText">Select user keywords...</span>
                            </button>
                            <ul class="dropdown-menu w-100" aria-labelledby="userKeywordsDropdown">
                                <% userKeywords.forEach(kw => {
                                    const id = typeof kw === 'object' ? kw.id : kw;
                                    const label = typeof kw === 'object' ? kw.label : kw;
                                    const description = typeof kw === 'object' ? kw.description : '';
                                %>
                                    <li>
                                        <div class="dropdown-item-checkbox px-3 py-2">
                                            <div class="form-check">
                                                <input class="form-check-input user-keyword-checkbox" type="checkbox" name="user-keywords[]" value="<%= id %>" data-label="<%= label %>" id="user-keyword-<%= id %>">
                                                <label class="form-check-label ms-2" for="user-keyword-<%= id %>"<% if (description) { %> title="<%= description %>"<% } %>><%= label %><% if (description) { %> <small class="text-muted">- <%= description %></small><% } %></label>
                                            </div>
                                        </div>
                                    </li>
                                <% }); %>
                            </ul>
                        </div>
                    </div>
                </div>

                <!-- Author Lock + Private (any creator may set these on their own new page) -->
                <div class="row mb-3">
                    <div class="col-md-3">
                        <label style="font-weight:bold;">Author Lock: <%- include('_help-icon', { slug: 'author-lock', label: 'Author Lock' }) %></label>
                        <div class="form-check mt-2">
                            <input class="form-check-input" type="checkbox" name="author-lock" id="authorLock" value="true">
                            <label class="form-check-label" for="authorLock" title="Only the page author and admins can edit this page">
                                <i class="fas fa-lock"></i> Locked
                            </label>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <label style="font-weight:bold;">Private: <%- include('_help-icon', { slug: 'page-private', label: 'Private Page' }) %></label>
                        <div class="form-check mt-2">
                            <input class="form-check-input" type="checkbox" name="private" id="privateFlag" value="true">
                            <label class="form-check-label" for="privateFlag" title="Only the page author and admins can view this page">
                                <i class="fas fa-eye-slash"></i> Private
                            </label>
                        </div>
                    </div>
                </div>

                <!-- Submit Buttons -->
                <div class="d-flex gap-3 mb-3">
                    <button type="submit" class="btn btn-primary">Create Page</button>
                    <a href="/" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    // Form submission validation for system-category and user-keywords
    document.querySelector('form').addEventListener('submit', function(e) {
        // Validate system-category (should always be present)
        const systemCategory = document.getElementById('systemCategory').value;
        if (!systemCategory) {
            e.preventDefault();
            alert('System category is required');
            return false;
        }
        // Validate user-keywords (max <%= maxUserKeywords %>, optional)
        const checkedKeywords = document.querySelectorAll('.user-keyword-checkbox:checked').length;
        if (checkedKeywords > <%= maxUserKeywords %>) {
            e.preventDefault();
            alert('Maximum <%= maxUserKeywords %> user keywords allowed');
            return false;
        }
    });
    
    // User keyword limitation
    const userKeywordCheckboxes = document.querySelectorAll('.user-keyword-checkbox');
    userKeywordCheckboxes.forEach(checkbox => {
        checkbox.addEventListener('change', function() {
            const checkedCount = document.querySelectorAll('.user-keyword-checkbox:checked').length;
            if (checkedCount > <%= maxUserKeywords %>) {
                this.checked = false;
                alert('Maximum <%= maxUserKeywords %> user keywords allowed');
            }
            updateSelectedUserKeywordsText();
        });
    });
    
    // Removed obsolete category UI update
    
    function updateSelectedUserKeywordsText() {
        const checkedKeywords = Array.from(document.querySelectorAll('.user-keyword-checkbox:checked')).map(cb => cb.dataset.label || cb.value);
        const text = checkedKeywords.length > 0 ? checkedKeywords.join(', ') : 'Select user keywords...';
        document.getElementById('selectedUserKeywordsText').textContent = text;
    }
    
    // Initialize dropdown text
    updateSelectedUserKeywordsText();
</script>

<%- include('footer') %>
