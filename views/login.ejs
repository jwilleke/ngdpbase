<%- include('header', { pageName: '' }) %>

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-4">
            <div class="card mt-5">
                <div class="card-header text-center">
                    <h4>Login to <%= typeof applicationName !== 'undefined' ? applicationName : 'ngdpbase' %></h4>
                </div>
                <div class="card-body">
                    <% if (error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                    <% } %>
                    
                    <% if (typeof success !== 'undefined' && success) { %>
                        <div class="alert alert-success">
                            <%= success %>
                        </div>
                    <% } %>
                    
                    <% if (googleOIDCEnabled) { %>
                    <form action="/auth/oauth/google" method="POST">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <input type="hidden" name="redirect" value="<%= redirect || '/' %>">
                        <button type="submit" class="btn btn-light w-100" style="border:1px solid #dadce0; font-size:0.95rem;">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="18" height="18" style="margin-right:8px;vertical-align:middle;">
                                <path fill="#EA4335" d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"/>
                                <path fill="#4285F4" d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"/>
                                <path fill="#FBBC05" d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z"/>
                                <path fill="#34A853" d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.18 1.48-4.97 2.31-8.16 2.31-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"/>
                            </svg>
                            Sign in with Google
                        </button>
                    </form>
                    <% } %>
                    <% if (passwordAuthEnabled || !googleOIDCEnabled) { %>
                    <% if (googleOIDCEnabled) { %><hr class="my-3"><% } %>
                    <form action="/login" method="POST">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <input type="hidden" name="redirect" value="<%= redirect || '/' %>">
                        <div class="form-group mb-3">
                            <label for="username">Username:</label>
                            <input type="text" class="form-control" id="username" name="username" required autofocus>
                        </div>
                        <div class="form-group mb-3">
                            <label for="password">Password:</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Login</button>
                    </form>
                    <% if (magicLinkEnabled) { %>
                    <hr>
                    <p class="text-center text-muted small mb-2">Or sign in without a password</p>
                    <% if (magic === 'sent') { %>
                        <div class="alert alert-success">
                            If that email is registered, a magic link has been sent. Check your inbox.
                        </div>
                    <% } else { %>
                        <form action="/auth/magic-link" method="POST">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <input type="hidden" name="redirect" value="<%= redirect || '/' %>">
                            <div class="input-group">
                                <input type="email" class="form-control" name="email"
                                       placeholder="your@email.com" required>
                                <button type="submit" class="btn btn-outline-secondary">Email me a link</button>
                            </div>
                        </form>
                    <% } %>
                    <% } %>
                    <% } %>

                    <hr>
                    <div class="text-center">
                        <p class="mb-0"><a href="/">← Back to home</a></p>
                    </div>
                </div>
                
                <div class="card-footer text-center">
                    <small class="text-muted">
                        Default admin: username = <code>admin</code>, password = <code>admin123</code>
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('footer') %>
