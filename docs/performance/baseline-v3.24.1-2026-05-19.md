# Performance baseline — v3.24.1

Captured 2026-05-19 on instance `jimstest` (port 3000).

## Snapshot

| Metric | Value |
| --- | --- |
| Version | v3.24.1 |
| Pages on disk | 17527 |
| Resident memory (idle) | 1422.2 MB |
| Memory source | telemetry |
| Heap used | 703.1 MB |
| Heap total | 887.4 MB |
| Engine init duration (mean from telemetry) | 4731 ms |
| Link-graph entries | 36348 |

## Response times (mean across 10 samples)

| Route | Time |
| --- | --- |
| `/` | 30 ms |
| `/view/Welcome` | 20 ms |
| `/search?q=test` | 149 ms |
| `/login` | 19 ms |

## Methodology

Generated by `scripts/baseline-profile.sh`. Memory read from `/metrics` (`MetricsManager` Prometheus exporter) when reachable — single source of truth across live telemetry and per-release baselines (#610). Falls back to `pm2 jlist` when telemetry is off (default). The "Memory source" row reflects which path was used. Heap used/total and engine init duration come from telemetry only. Override the metrics endpoint with `BASELINE_METRICS_URL`; force the pm2 path with `BASELINE_METRICS_DISABLE=1`. Response times measured with `curl -L --time_total` (follows redirects) against a warm server, 10 iterations per route, mean reported. The unauth section samples routes that don't require login. Authenticated routes are sampled separately when `BASELINE_USER`+`BASELINE_PASS` env vars are set — `/admin` only renders the dashboard for admin-level creds; for non-admin users it 302s to /login and the timing reflects the redirect. Cold-start time measured only when invoked with `--cold-start` — wraps `./server.sh stop` + `./server.sh start` and waits for the "Engine initialized" marker in `pm2-out.log`.

Re-run on each release to track drift over time. Compare against the previous baseline file in `docs/performance/` to spot regressions.

## Drift vs baseline-v3.24.0-2026-05-19.md

⚠️  **Regression candidate(s):** /search?q=test (+263.4%, +108ms)

| Metric | Previous | New | Δ |
| --- | --- | --- | --- |
| Memory (idle) | 1332.6 MB | 1422.2 MB | +89.6 MB (+6.7%) |
| `/` | 37 ms | 30 ms | -7 ms (-18.9%) |
| `/view/Welcome` | 27 ms | 20 ms | -7 ms (-25.9%) |
| `/search?q=test` | 41 ms | 149 ms | +108 ms (+263.4%) |
| `/login` | 25 ms | 19 ms | -6 ms (-24.0%) |

Thresholds (override via env): memory 25% / route 50% AND 50ms (both must trip).
